#include <stdio.h>
#include "common.h"
#include "macros.h"

int main(int argc, char *argv[]) {
    printf("Emergency Client starting...\n");

    // In future steps, we will:
    // 1. Parse command-line arguments.
    // 2. Connect to the message queue.
    // 3. Construct and send an emergency_request_t message.

    printf("Emergency Client finished.\n");

    return 0;
}